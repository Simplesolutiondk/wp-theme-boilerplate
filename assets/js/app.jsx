// Import SASS
import '../scss/main.scss'

/**
 * You won't need to import React and ReactDOM, this is being loaded via. CDN.
 * I've created an EXAMPLE to create a basic class-component:
 * export default class App extends React.Component {
 *  render() {
 *      return <h1>HELLO FROM REACT!</h1>;
 *  }
 * }
 * ReactDOM.render(<h1>HELLO FROM REACT!</h1>, document.querySelector('#app-root'));
 */